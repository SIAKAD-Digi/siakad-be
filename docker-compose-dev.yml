services:
  app:
    image: siakad-digi-be-dev:latest
    container_name: siakad-digi-be-dev
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always

